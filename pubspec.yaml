name: adaptive_dashboard_shell
description: Adaptive dashboard shell with collapsible sidebar and mobile bottom nav.
version: 1.1.2
repository: https://github.com/subash9860/adaptive_dashboard_shell
homepage: https://github.com/subash9860/adaptive_dashboard_shell

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
